{
  "name": "bot-whatsapp",
  "version": "1.0.0",
  "description": "Bot de atendimento humanizado",
  "main": "index.js",
"scripts": {
  "start": "node index.js",
  "build": "npm install && PUPPETEER_CACHE_DIR=./.cache/puppeteer npx puppeteer browsers install chrome"
}
  "dependencies": {
    "whatsapp-web.js": "^1.23.0",
    "qrcode-terminal": "^0.12.0",
    "express": "^4.18.2",
    "cors": "^2.8.5"
  }
}
